# Stage-B gate

No conditional-covariance experiment is implemented here. This directory is
intentionally gated until `reports/STAGE_A_MODEL_READINESS_DECISION.md` says
`READY FOR STAGE B` on real-data evidence.

